#gh:gh_sase-org__sase #fork:jb Can you now help me add support for two new phase sizes, `xsmall` and `xlarge`?

- Make sure that we tell agents, via the explanation text that is output by the `sase plan validate` command's `--explain` CLI option how these new sizes are meant to be used:
  - `TODO`
- #beau

#plan #m_opus