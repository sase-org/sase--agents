#gh:gh_sase-org__sase #fork:athena.sase-8v.land Can you help me remove any backward
compatibility logic that we left around (this is the only machine I've been
using with sase for weeks and I am its only user) and just strip the `athena.`
machine hood prefix from all old Agent data files (I'm not sure if this is the
only fix that needs to be applied, so you should look into this--perform any
work to the sase agent data that is stored on this disk that is needed to remove
this backward compatibility code)? Make sure you don't break anything on this
machine though!

#plan