#gh:gh_sase-org__sase I think we recently enabled the `epic_resume` check, which is failing (see #sshot). We recently enabled a feature flag for this functionality for the first time and I think we even removed the feature flag. I have recently decided that I don't like this functionality however. Can you help me completely remove it (make sure you trace this functionality back to the original plan file)? #plan