%auto:tale
#gh:gh_sase-org__sase Can you help me add support to the `v` (view) keymap for failed tool calls in the "SLOW TOOL CALLS" section of the agent metadata panel on the "Agents" tab of the `sase ace` TUI?

- If the user selects the hint associated with a failed slow tool call, a file should be built on the fly that contains useful metadata about the tool call and the failed tool output.
- #beau

#plan #m_fable %w:0z