#gh:gh_sase-org__sase Can you help me add an indicatior somewhere for the prompt input widget when
the search functionality is active (which is triggered via the `/` keymap) to show how
many matches there are and which match is currently selected (ex: `3/10`)? #beau

#plan %m:opus