#gh:gh_sase-org__sase Any agent house that does not belong in a panel sub-group (the `hi` agent in #sshot does not belong to any sub-group, for example--unlike `hk` and `hk.f0`, which both belong to the `hk` sub-group), then it should be listed above all sub-groups in that group (e.g. `hi` and `hh` should both be listed somewhere above the `hk` group). Can you help me diagnose the root cause of this issue and fix it? #plan