#gh:gh_sase-org__sase Can you help me make sure that, when a sase monitor finishes and a next action was specified, that we escape the entire prompt (as much of it as we can without breaking things at least) of the command using the `%xprompts_enabled` directive? This will prevent agents from accidently introducing validation errors (e.g. by not escaping directive names, for example). #if_not_plan