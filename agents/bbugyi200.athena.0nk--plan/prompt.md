#gh:gh_sase-org__sase Can you help me add support for bulk selecting multiple workspaces to open from
the "Tmux workspaces" panel (triggered via the `t` keymap on the "Agents" tab) by adding
a new `m` keymap that allows you to select (i.e. mark) multiple workspaces to open?
#plan %m:gpt-5.6-sol