#gh:gh_sase-org__sase Can you help me implement feature flags for sase?

- See the feature_flag_lifecycle_governance.md file in the research sidecar repo for
  context and inspiration. With that said, make sure that requirements in this prompt
  are prioritized (i.e. don't implement the research recommendations exactly if they
  contradict this prompt).
- I like the idea of using dedicated removal beads. Let's add a new sase bead type for
  this: "flag"
- Make sure all UI services that display beads have a unique and visually appealing way
  of displaying flag beads.
- When a sase flag bead's trigger is met, an excellent sase gate should be used to
  prompt the user on how they want to handle it. See the sase gate that we use for task
  beads for inspiration.
- Add a new sase/memory/sase_flags.md long-term memory file that gets generated by the
  `sase init` command (like we do for the sase/memory/sase_beads.md file, for example).
  This memory file should desribe the best practices for creating and removing/closing
  feature flags and flag beads. Reference this memory in the sase/memory/sase.md memory
  file file, with brief instructions on when an agent SHOULD/MUST create new feature
  flags.
- Make sure the sase/memory/sase.md changes you make and the instructions in the (and
  description for the) new sase/memory/sase_flags.md are excellent but concise. Remember
  that every token added to context either helps or hurts us.
- #beau

#plan #m_opus