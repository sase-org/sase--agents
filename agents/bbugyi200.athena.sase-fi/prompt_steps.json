[{"file_name":"prompt_step_commit___emit_commit_id.json","marker":{"embedded_workflow_name":"commit","hidden":true,"is_pre_prompt_step":false,"output":{},"parent_step_index":0,"parent_total_steps":1,"status":"completed","step_index":8,"step_name":"_emit_commit_id","step_source":"import json, os\nartifacts_dir = os.environ.get(\"SASE_ARTIFACTS_DIR\", \"\")\nresult_file = os.path.join(artifacts_dir, \"commit_result.json\") if artifacts_dir else \"\"\nif result_file and os.path.isfile(result_file):\n    with open(result_file) as f:\n        d = json.load(f)\n    entry_id = d.get(\"entry_id\", \"\") or \"\"\n    if entry_id:\n        print(f\"meta_commit_id={entry_id}\")\n","step_type":"python","total_steps":1,"workflow_name":"tmp_260806_154414"}},{"file_name":"prompt_step_commit___has_commit_result.json","marker":{"embedded_workflow_name":"commit","hidden":true,"is_pre_prompt_step":false,"output":{"exists":true},"parent_step_index":0,"parent_total_steps":1,"status":"completed","step_index":6,"step_name":"_has_commit_result","step_source":"import os\nartifacts_dir = os.environ.get(\"SASE_ARTIFACTS_DIR\", \"\")\npath = os.path.join(artifacts_dir, \"commit_result.json\") if artifacts_dir else \"\"\nprint(f\"exists={'true' if path and os.path.isfile(path) else 'false'}\")\n","step_type":"python","total_steps":1,"workflow_name":"tmp_260806_154414"}},{"file_name":"prompt_step_commit__check_changes.json","marker":{"embedded_workflow_name":"commit","hidden":true,"is_pre_prompt_step":false,"output":{"has_changes":false},"parent_step_index":0,"parent_total_steps":1,"status":"completed","step_index":5,"step_name":"check_changes","step_source":"import os\nfrom sase.vcs_provider import get_vcs_provider\nprovider = get_vcs_provider(os.getcwd())\nok, changes = provider.has_local_changes(os.getcwd())\nhas_changes = ok and changes is not None and len(changes) > 0\nprint(f\"has_changes={'true' if has_changes else 'false'}\")\n","step_type":"python","total_steps":1,"workflow_name":"tmp_260806_154414"}},{"file_name":"prompt_step_commit__create.json","marker":{"embedded_workflow_name":"commit","hidden":true,"is_pre_prompt_step":false,"output":{"new_commit":"2ac967d78","success":true},"parent_step_index":0,"parent_total_steps":1,"status":"completed","step_index":7,"step_name":"create","step_source":"import json, os\nartifacts_dir = os.environ.get(\"SASE_ARTIFACTS_DIR\", \"\")\nresult_file = os.path.join(artifacts_dir, \"commit_result.json\") if artifacts_dir else \"\"\nif result_file and os.path.isfile(result_file):\n    with open(result_file) as f:\n        d = json.load(f)\n    print(\"success=true\")\n    print(f\"new_commit={d.get('result', '') or ''}\")\nelse:\n    print(\"success=false\")\n","step_type":"python","total_steps":1,"workflow_name":"tmp_260806_154414"}},{"file_name":"prompt_step_commit__report.json","marker":{"embedded_workflow_name":"commit","hidden":true,"is_pre_prompt_step":false,"output":{"diff_path":"/home/bryan/.sase/projects/gh_sase-org__sase/artifacts/ace-run/202608/06/20260806154327/commit_diffs/004.diff","meta_commit_message":"fix(agents-sync): retry deferred prompt archives on the full sync path\n\nA prompt archive that could not be written at commit time is queued as an\nagent-hood outbox request, and the commit-time drain rebuilds it. The full\nsync path had no such step: `sync_agents` acknowledged a queued request as\nsoon as the agent's README materialized, so `sase agent sync` cleared the\nrequest and discarded the owed prompt archive with it.\n\nThe export pass now builds the hood inventory once, reuses it for hood\nreconciliation, and rebuilds the prompt archives owed by the requests it is\nabout to acknowledge. A request whose archive cannot be rebuilt is left\nqueued and retryable rather than retired.\n\nExtract the drain's retry into `agents_sync/prompt_archive/deferred.py` so\nboth paths share it, stage `prompts/` and `artifacts/` in the sync commit,\nand clean them at both transaction boundaries. At commit time, a prompt\narchive that could not even be queued now fails the step with a\n`sase commit --resume` hint instead of a dropped warning.\n\nSASE_BEAD=[sase-fi][1]\nSASE_AGENT=[bbugyi200.athena.sase-fi][2]\n\n[1]: https://github.com/sase-org/sase--beads/blob/main/pages/sase-fi/README.md\n[2]: https://github.com/sase-org/sase--agents/blob/main/agents/bbugyi200.athena.sase-fi/README.md","meta_new_commit":"2ac967d78"},"parent_step_index":0,"parent_total_steps":1,"status":"completed","step_index":9,"step_name":"report","step_source":"import json, os\nartifacts_dir = os.environ.get(\"SASE_ARTIFACTS_DIR\", \"\")\nresult_file = os.path.join(artifacts_dir, \"commit_result.json\") if artifacts_dir else \"\"\nif not result_file or not os.path.isfile(result_file):\n    print(\"{}\")\nelse:\n    with open(result_file) as f:\n        d = json.load(f)\n    out: dict[str, str] = {}\n    result = d.get(\"result\", \"\") or \"\"\n    message = d.get(\"message\", \"\") or \"\"\n    changespec_name = d.get(\"changespec_name\", \"\") or \"\"\n    name = d.get(\"name\", \"\") or \"\"\n    diff_path = d.get(\"diff_path\", \"\") or \"\"\n    if result:\n        out[\"meta_new_commit\"] = result\n    if message:\n        out[\"meta_commit_message\"] = message\n    cs = changespec_name or name\n    if cs:\n        out[\"meta_changespec\"] = cs\n    if diff_path:\n        out[\"diff_path\"] = diff_path\n    print(json.dumps(out))\n","step_type":"python","total_steps":1,"workflow_name":"tmp_260806_154414"}},{"file_name":"prompt_step_gh__checkout.json","marker":{"embedded_workflow_name":"gh","hidden":false,"is_pre_prompt_step":true,"output":{"head_before":"5da193482e4f755413418330e7f5c3e681ccf73f","success":true},"parent_step_index":0,"parent_total_steps":1,"status":"completed","step_index":2,"step_name":"checkout","step_source":"import os\nimport subprocess\nimport sys\n\nfrom sase.vcs_provider import get_vcs_provider\n\ncheckout_target = {{ setup.checkout_target | tojson }}\nproject_name = {{ setup.project_name | tojson }}\ngh_ref = {{ gh_ref | tojson }}\n\nprovider = get_vcs_provider(os.getcwd())\n\n# Checkout the PR branch so that subsequent diffs are relative to the\n# PR tip rather than the default branch.  Try checkout_target first\n# (works for default-branch refs like origin/main), then fall back to\n# ChangeSpec name -> branch name conversion.\nis_changespec = checkout_target == f\"origin/{gh_ref}\"\nok, err = provider.checkout(checkout_target, os.getcwd())\nif not ok:\n    if is_changespec:\n        # ChangeSpec ref — resolve name to branch name\n        resolved = provider.resolve_revision(\n            gh_ref, project_name, os.getcwd()\n        )\n        ok, err = provider.checkout(resolved, os.getcwd())\n    else:\n        # Project/repo ref — try common default branch names\n        for fallback in [\"master\", \"main\"]:\n            ok, err = provider.checkout(fallback, os.getcwd())\n            if ok:\n                break\n    if not ok:\n        print(\n            f\"Warning: checkout failed: {err}\",\n            file=sys.stderr,\n        )\n\n# Sync with remote after checkout\nsubprocess.run(\n    [\"git\", \"pull\", \"--rebase\", \"--quiet\"],\n    capture_output=True,\n    text=True,\n    check=False,\n)\n\n# Record HEAD for the diff step\nresult = subprocess.run(\n    [\"git\", \"rev-parse\", \"HEAD\"],\n    capture_output=True,\n    text=True,\n    check=False,\n)\nhead = result.stdout.strip() if result.returncode == 0 else \"\"\nprint(f\"head_before={head}\")\nprint(\"success=true\")\n","step_type":"python","total_steps":1,"workflow_name":"tmp_260806_154414"}},{"file_name":"prompt_step_gh__diff.json","marker":{"embedded_workflow_name":"gh","hidden":false,"is_pre_prompt_step":false,"output":{"diff_path":"/home/bryan/tmp/sase/gh-diffs/sase-gh-rkAyC8.diff","meta_commit_message":"fix(agents-sync): retry deferred prompt archives on the full sync path"},"parent_step_index":0,"parent_total_steps":1,"status":"completed","step_index":11,"step_name":"diff","step_source":"head_before=\"{{ checkout.head_before }}\"\nif [ -z \"$head_before\" ]; then\n  exit 0\nfi\nhead_now=$(git rev-parse HEAD)\ndiff_dir=\"${SASE_TMPDIR:-${SASE_HOME:-$HOME/.sase}/tmp}/gh-diffs\"\nmkdir -p \"$diff_dir\"\ndiff_file=$(mktemp \"$diff_dir/sase-gh-XXXXXX.diff\")\nif [ \"$head_now\" != \"$head_before\" ]; then\n  # Commits were made — show only the last commit's diff\n  git diff HEAD~1 HEAD > \"$diff_file\" 2>/dev/null\n  commit_msg=$(git log -1 --format='%s' HEAD)\n  echo \"meta_commit_message=$commit_msg\"\nelse\n  # No commits — show uncommitted changes\n  git diff HEAD > \"$diff_file\" 2>/dev/null\n  git ls-files --others --exclude-standard -z 2>/dev/null \\\n    | while IFS= read -r -d '' f; do\n        git diff --no-index /dev/null \"$f\" 2>/dev/null\n      done >> \"$diff_file\"\nfi\nif [ -s \"$diff_file\" ]; then\n  echo \"diff_path=$diff_file\"\nelse\n  rm -f \"$diff_file\"\nfi\n","step_type":"bash","total_steps":1,"workflow_name":"tmp_260806_154414"}},{"file_name":"prompt_step_gh__prepare.json","marker":{"embedded_workflow_name":"gh","hidden":false,"is_pre_prompt_step":true,"output":{"stashed":false,"success":true},"parent_step_index":0,"parent_total_steps":1,"status":"completed","step_index":1,"step_name":"prepare","step_source":"# Stash any uncommitted changes (including untracked files)\nif ! git diff --quiet HEAD 2>/dev/null || [ -n \"$(git ls-files --others --exclude-standard)\" ]; then\n  git stash push --include-untracked -m \"gh-workflow-$(date +%s)\" --quiet\n  echo \"stashed=true\"\nelse\n  echo \"stashed=false\"\nfi\ngit fetch origin --quiet 2>&1 || true\ngit pull --rebase --quiet 2>/dev/null || true\necho \"success=true\"\n","step_type":"bash","total_steps":1,"workflow_name":"tmp_260806_154414"}},{"file_name":"prompt_step_gh__setup.json","marker":{"embedded_workflow_name":"gh","hidden":false,"is_pre_prompt_step":true,"output":{"checkout_target":"origin/master","meta_workspace":"16","primary_workspace_dir":"/home/bryan/projects/github/sase-org/sase/","project_file":"/home/bryan/.sase/projects/gh_sase-org__sase/gh_sase-org__sase.sase","project_name":"gh_sase-org__sase","should_release":false,"workflow_name":"gh-gh_sase-org__sase","workspace_dir":"/home/bryan/.local/state/sase/workspaces/sase-org/sase/sase_16/","workspace_num":16},"parent_step_index":0,"parent_total_steps":1,"status":"completed","step_index":0,"step_name":"setup","step_source":"from sase_github.scripts.gh_setup import main\nmain(\n    gh_ref={{ gh_ref | tojson }},\n    n={{ n | tojson }},\n    release={{ release | tojson }},\n    workflow_label={{ workflow_label | tojson }},\n)\n","step_type":"python","total_steps":1,"workflow_name":"tmp_260806_154414"}},{"file_name":"prompt_step_main.json","marker":{"embedded_workflow_name":null,"hidden":false,"is_pre_prompt_step":false,"output":{"_raw":"I'll start by looking at the bead.\n\nNow update `commit_publication_transaction.py` to use the shared helper.\n\nNow the full-sync path in `git_sync.py`.\n\nNow add the helper and imports in `git_sync.py`.\n\nNow add the `_restore_deferred_prompt_archives` helper.\n\nNow update `git_sync.py` to thread the failures through.\n\nNow wire the caller in `sync_agents`.\n\nNow the commit-time message in `workflow_publication.py`.\n\nNow the full check.\n\nDone. `sase-fi` is closed.\n\n## What was actually broken\n\nThe bead described the prompt archive as having no durable retry at all, but the real gap was narrower and worse-behaved: `publish_prompt_archive` *does* enqueue an agent-hood outbox request before touching the lock, and the commit-time drain (`_prepare_prompt_archive_retry`) *does* rebuild the deferred archive. The hole was the full-sync path — `sync_agents` acknowledged a queued request as soon as `agents/<global>/README.md` materialized, while `_integrate_export_pass` never rebuilt the prompt. So `sase agent sync -p sase` cleared the request and took the owed prompt with it. That matches the reported symptom exactly: sase-fa.4/.5 vanished after a later successful sync.\n\n## Changes\n\n- **`src/sase/agents_sync/prompt_archive/deferred.py`** (new) — the drain's prompt-archive retry, extracted so both paths share it; adds a non-raising `restore_deferred_prompt_archives` that reports per-request failures keyed by logical key.\n- **`commit_publication_transaction.py`** — now uses the shared helper instead of its private copies.\n- **`git_sync.py`** — the full sync's export pass builds the inventory once, passes it to hood reconciliation, and rebuilds the archives owed by the requests it is about to acknowledge. A request whose archive could not be rebuilt is *not* acknowledged and, unlike a non-materializing page, stays retryable rather than being retired.\n- **`git_sync_transaction.py`** — stages `prompts`/`artifacts` in the sync commit and cleans them alongside the payload at both transaction boundaries.\n- **`workflow_publication.py`** — a prompt archive that could not even be queued now fails the step with a `sase commit --resume` hint (matching agent-hood publication) instead of a dropped warning; a deferred one warns that it retries with agent publication.\n- **`docs/agents_sidecar.md`** — documents that the hood request carries the prompt.\n\n## Verification\n\n`tests/agents_sync/test_deferred_prompt_archive.py` drives a genuine lock-busy defer over real git sidecars, then asserts the archive reaches the remote on the next sync and only then is the request acknowledged; a second test asserts an unrebuildable archive keeps the request queued and non-terminal. I confirmed both fail without the fix. Two new commit-workflow tests cover the queued and unqueueable messaging.\n\n`just check-full` passed except two tests that also failed for reasons unrelated to this change and pass in isolation: the bead mutation-lock one is already fixed on `origin/master` by `5a1980363` (my checkout is 6 commits behind), and the ACE TUI codeblock-highlight one I recorded as a +1 on `sase-ct` — it's a different test than that bead names and isn't covered by `bde727ecc`, so that +1 moved `sase-ct` back to READY.\n\nI made no commit, branch, or PR.\n\nCommitted and pushed.\n\n- **Bead**: `sase-fi` was already closed; re-running `sase bead close` reported \"Already closed\" and appended the verification note without an unpublished-close error, so the close is published.\n- **Commit**: `2ac967d78 fix(agents-sync): retry deferred prompt archives on the full sync path`, staging exactly the eight listed files (including the two new ones). The `sase_git_commit` invocation exceeded my 2-minute command ceiling and was killed, so I verified the outcome directly: `git merge-base --is-ancestor` confirms the commit is on `origin/master`, and the working tree is clean. The only side effect of the kill was the leftover `.sase/commit_message.md`, which I removed.\n\nThe change gives the full-sync path (`sync_agents`) the prompt-archive rebuild it was missing: it now rebuilds the archives owed by the outbox requests it is about to acknowledge, and leaves a request queued and retryable if the rebuild fails, instead of acknowledging on README materialization alone and discarding the owed prompt.","diff_path":"/home/bryan/.sase/projects/gh_sase-org__sase/artifacts/ace-run/202608/06/20260806154327/commit_diffs/004.diff","meta_commit_message":"fix(agents-sync): retry deferred prompt archives on the full sync path","meta_new_commit":"2ac967d78","meta_workspace":"16"},"parent_step_index":null,"parent_total_steps":null,"status":"completed","step_index":0,"step_name":"main","step_source":null,"step_type":"agent","total_steps":1,"workflow_name":"tmp_260806_154414"}},{"file_name":"prompt_step_prdd__check_branch.json","marker":{"embedded_workflow_name":"prdd","hidden":false,"is_pre_prompt_step":true,"output":{"should_inject_prompt_part":false},"parent_step_index":0,"parent_total_steps":1,"status":"completed","step_index":3,"step_name":"check_branch","step_source":"branch=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo \"\")\nif [ \"$branch\" = \"master\" ] || [ \"$branch\" = \"main\" ] || [ \"$branch\" = \"\" ]; then\n  echo \"should_inject_prompt_part=false\"\nelse\n  echo \"should_inject_prompt_part=true\"\nfi\n","step_type":"bash","total_steps":1,"workflow_name":"tmp_260806_154414"}}]
