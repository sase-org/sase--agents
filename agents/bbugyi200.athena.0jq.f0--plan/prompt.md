#gh:gh_sase-org__sase #fork:0jq The usage windows look good in the header with the Textual app title, but can you make the title always centered on th the screen? Also, let's start emphasizing the name of the usage window (ex: "fable") too when `<N>` equals `0`, instead of just emphasizing `<N>% <duration>`.

#plan