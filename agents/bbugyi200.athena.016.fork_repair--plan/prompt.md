%model:codex/gpt-6-astra@xhigh
%id:016.fork_repair
#gh:gh_sase-org__sase
%auto
Repair the family-history growth defect discovered by supervision epoch016. This bounded repair is explicitly authorized by Bryan's ongoing instruction to investigate/fix/commit issues preventing supervised agents from completing. FIRST read sase-xu latest cycle5 notes and active sase-kp's latest DISCOVERED ISSUE note, then audited artifact file:explicit:84baef411b134a607333b6f2. Do not fork family016 or x7: their already-expanded histories recursively repeat; use the board and artifact context on demand.

Concrete failure: sase-x7.4.r0--6 failed at provider admission on 2026-09-07T05:19Z input_too_large, actual1316146 chars > max1048576; generated prompt has32 Previous Conversations headings while stored raw monitor handoff is3437 chars. Supervisor016--5 already has16 headings/335160 chars, so repair must prevent future monitor successors failing. Candidate source path: src/sase/history/chat_fork/family.py calls load_chat_for_resume for every member; chat_resume.py preserves previously expanded history inside each Prompt when raw fork directives are gone. Confirm root cause, then remove recursive re-embedding while retaining each unique turn, explicit source/family attribution and ordering, failed/incomplete disclaimers, proc output as untrusted, and human gate decisions. Do not simply raise provider limits or silently truncate unique user requirements. Add a meaningful multi-generation regression that reproduces old growth and proves appropriately bounded/linear new history. Examine regular fork, monitor, pipe and gate callers for necessary parity. Shared backend/domain behavior must follow the Rust core boundary; open other repos via /sase_repo. Keep changes narrowly about this confirmed defect.

sase_new_task triage already searched bug/all-type catalogs, swept every last-week task and inspected all26 active epic scopes plus kp.4/12. kp.4 explicitly owns monitor fork continuation and kp remains in_progress: evidence is routed there, so do not create a duplicate task. xn is a different old core-floor issue. Read reference memories required for files touched; just install before checks in a fresh clone; just check is default and check-full must use monitor. If you need a plan, use a small bounded tale and fresh handoff rather than forking the failing old family. If long verification needs a monitor, keep its next prompt concise. All newly launched helpers/continuations must use codex/gpt-6-astra@xhigh; never use /sase_run or ask Bryan for launch permission. User has authorized repairs/commits; use /sase_git_commit for any manual commit and /sase_final for host-owned declaration. Publish the repair and verify actual finalizer/commit evidence when available. Do not change x7/xq/federation workers' checkouts.

Append concise findings, tests, commit IDs and remaining limitations to BOTH sase-kp and sase-xu. Explicitly tell next supervisor whether the installed runtime has the fix and whether family016 can safely continue. A local patch or submitted declaration is not proof of publication. Family016 successors will supervise this repair to completion; do not close kp or xu. Pending research read-link metadata belongs to supervisor workspace, not this repair.