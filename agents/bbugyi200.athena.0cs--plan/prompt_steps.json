[{"file_name":"prompt_step_gh__checkout.json","marker":{"embedded_workflow_name":"gh","hidden":false,"is_pre_prompt_step":true,"output":{"head_before":"cbea4f23b2552de812952ec474035d59923a4d2c","success":true},"parent_step_index":0,"parent_total_steps":1,"status":"completed","step_index":2,"step_name":"checkout","step_source":"import os\nimport subprocess\nimport sys\n\nfrom sase.vcs_provider import get_vcs_provider\n\ncheckout_target = {{ setup.checkout_target | tojson }}\nproject_name = {{ setup.project_name | tojson }}\ngh_ref = {{ gh_ref | tojson }}\n\nprovider = get_vcs_provider(os.getcwd())\n\n# Checkout the PR branch so that subsequent diffs are relative to the\n# PR tip rather than the default branch. Try checkout_target first\n# (works for default-branch refs like origin/main), then fall back to\n# Patch name -> branch name conversion.\nis_patch = checkout_target == f\"origin/{gh_ref}\"\nok, err = provider.checkout(checkout_target, os.getcwd())\nif not ok:\n    if is_patch:\n        # Patch ref — resolve name to branch name\n        resolved = provider.resolve_revision(\n            gh_ref, project_name, os.getcwd()\n        )\n        ok, err = provider.checkout(resolved, os.getcwd())\n    else:\n        # Project/repo ref — try common default branch names\n        for fallback in [\"master\", \"main\"]:\n            ok, err = provider.checkout(fallback, os.getcwd())\n            if ok:\n                break\n    if not ok:\n        print(\n            f\"Warning: checkout failed: {err}\",\n            file=sys.stderr,\n        )\n\n# Sync with remote after checkout\nsubprocess.run(\n    [\"git\", \"pull\", \"--rebase\", \"--quiet\"],\n    capture_output=True,\n    text=True,\n    check=False,\n)\n\n# Record HEAD for the diff step\nresult = subprocess.run(\n    [\"git\", \"rev-parse\", \"HEAD\"],\n    capture_output=True,\n    text=True,\n    check=False,\n)\nhead = result.stdout.strip() if result.returncode == 0 else \"\"\nprint(f\"head_before={head}\")\nprint(\"success=true\")\n","step_type":"python","total_steps":1,"workflow_name":"tmp_260824_135856"}},{"file_name":"prompt_step_gh__diff.json","marker":{"embedded_workflow_name":"gh","hidden":false,"is_pre_prompt_step":false,"output":{},"parent_step_index":0,"parent_total_steps":1,"status":"completed","step_index":4,"step_name":"diff","step_source":"head_before=\"{{ checkout.head_before }}\"\nif [ -z \"$head_before\" ]; then\n  exit 0\nfi\nhead_now=$(git rev-parse HEAD)\ndiff_dir=\"${SASE_TMPDIR:-${SASE_HOME:-$HOME/.sase}/tmp}/gh-diffs\"\nmkdir -p \"$diff_dir\"\ndiff_file=$(mktemp \"$diff_dir/sase-gh-XXXXXX.diff\")\nif [ \"$head_now\" != \"$head_before\" ]; then\n  # Commits were made — show only the last commit's diff\n  git diff HEAD~1 HEAD > \"$diff_file\" 2>/dev/null\n  commit_msg=$(git log -1 --format='%s' HEAD)\n  echo \"meta_commit_message=$commit_msg\"\nelse\n  # No commits — show uncommitted changes\n  git diff HEAD > \"$diff_file\" 2>/dev/null\n  git ls-files --others --exclude-standard -z 2>/dev/null \\\n    | while IFS= read -r -d '' f; do\n        git diff --no-index /dev/null \"$f\" 2>/dev/null\n      done >> \"$diff_file\"\nfi\nif [ -s \"$diff_file\" ]; then\n  echo \"diff_path=$diff_file\"\nelse\n  rm -f \"$diff_file\"\nfi\n","step_type":"bash","total_steps":1,"workflow_name":"tmp_260824_135856"}},{"file_name":"prompt_step_gh__prepare.json","marker":{"embedded_workflow_name":"gh","hidden":false,"is_pre_prompt_step":true,"output":{"stashed":false,"success":true},"parent_step_index":0,"parent_total_steps":1,"status":"completed","step_index":1,"step_name":"prepare","step_source":"# Stash any uncommitted changes (including untracked files)\nif ! git diff --quiet HEAD 2>/dev/null || [ -n \"$(git ls-files --others --exclude-standard)\" ]; then\n  git stash push --include-untracked -m \"gh-workflow-$(date +%s)\" --quiet\n  echo \"stashed=true\"\nelse\n  echo \"stashed=false\"\nfi\ngit fetch origin --quiet 2>&1 || true\ngit pull --rebase --quiet 2>/dev/null || true\necho \"success=true\"\n","step_type":"bash","total_steps":1,"workflow_name":"tmp_260824_135856"}},{"file_name":"prompt_step_gh__setup.json","marker":{"embedded_workflow_name":"gh","hidden":false,"is_pre_prompt_step":true,"output":{"checkout_target":"origin/master","meta_workspace":"23","primary_workspace_dir":"/home/bryan/projects/github/sase-org/sase/","project_file":"/home/bryan/.sase/projects/gh_sase-org__sase/gh_sase-org__sase.sase","project_name":"gh_sase-org__sase","should_release":false,"workflow_name":"gh-gh_sase-org__sase","workspace_dir":"/home/bryan/.local/state/sase/workspaces/sase-org/sase/sase_23/","workspace_num":23},"parent_step_index":0,"parent_total_steps":1,"status":"completed","step_index":0,"step_name":"setup","step_source":"from sase_github.scripts.gh_setup import main\nmain(\n    gh_ref={{ gh_ref | tojson }},\n    n={{ n | tojson }},\n    release={{ release | tojson }},\n    workflow_label={{ workflow_label | tojson }},\n)\n","step_type":"python","total_steps":1,"workflow_name":"tmp_260824_135856"}},{"file_name":"prompt_step_main.json","marker":{"embedded_workflow_name":null,"hidden":false,"is_pre_prompt_step":false,"output":{"_raw":"I’m using `/sase_plan` because you explicitly requested the project’s gated planning workflow. I’ll also use `/sase_memory_read` for the required xprompt/chop guidance, then inspect the launch path, author the appropriate plan tier, and iterate validation before proposing it. No files will be changed before the proposal.\n\nThe planning skill requires canonical size guidance before tier selection. Because this bug is in chop/xprompt launch behavior, I’m also loading the audited xprompt reference and the relevant glossary definitions before tracing the code.\n\nThe reference confirms the key invariant: a bare launch prompt normalizes to `#git:home`, while `#gh:sase` is the explicit project target. I’m now tracing where `toobig_split` constructs its structured follow-up launch and where that payload is normalized, including existing tests and similar chops that already preserve project context.\n\nThe configured chop is not owned by this checkout: it comes from the external `bugyi-chops` package at commit `22b3db5…`. Its result already carries `workspace: \"gh:sase-org/sase\"`, so the likely defect is a contract mismatch: Axe uses that field to allocate a workspace but does not prepend a launch xprompt, leaving the bare prompt to normalize to `#git:home`. I’m using `/sase_repo` now because project instructions require it before inspecting that external repository’s source and tests.\n\nThe external chop is already doing the right producer-side thing: every proposal’s `workspace` is `gh:sase-org/sase`, and SASE’s `scaffolded_prompt()` converts that to `#gh:sase-org/sase` before planning. The failure is downstream in typed admission: SASE resolves the correct project from the scaffolded batch, but dispatch rebuilds each admitted unit from a Rust-produced payload. I’m checking whether that per-unit rebuild drops the workspace ref, which would exactly explain the later `#git:home` normalization.\n\nThe same gap exists in the generic typed-admission dispatcher, not just Axe: `LaunchPlanWire.selected_project` is retained for conditions and procs, but an `AgentUnitWire` has no project/workspace field, and `agent_unit_dispatch_prompt()` reconstructs only identity/model/body. Existing tests verify directives but never verify an explicit VCS target survives dispatch. I’m opening `sase-core` via `/sase_repo` to inspect the planner/wire boundary before deciding whether the fix belongs in Rust, Python dispatch context, or both.\n\nThe live evidence matches the static trace: current `toobig-*` agents are registered under project `home`, and their stored prompt snippets contain an injected `#git:home`. The regression was introduced by yesterday’s typed-chop-admission change: pre-admission plans contain the correct workspace tag, but `make_axe_chop_agent_dispatcher()` rebuilds the post-condition prompt without reattaching `unit_meta[\"workspace\"]`.\n\nThis is a focused SASE-side fix; `bugyi-chops` already emits the correct workspace and `sase-core` already retains the selected project at plan level. I’m selecting a medium tale: one follow-up agent can restore the validated workspace prefix in the Axe dispatcher and add end-to-end regression coverage for both the ordinary and promoted-clan-member paths.\n\nI’m authoring the tale now. The plan will keep the change narrowly at the failing boundary: preserve the chop proposal’s already-validated workspace when reconstructing a typed AgentUnit dispatch prompt, fail closed if durable metadata cannot provide it, and prove `#gh:sase` survives both normal admission and clan-declarer promotion. No production files are being edited.\n\nThe first `--explain` validation passed with zero warnings, so the chosen tale schema and medium size are accepted. I’m performing the required clean revalidation now; if it remains valid, the next action will be `sase plan propose`, which intentionally hands this turn to the approval workflow.","meta_workspace":"23"},"parent_step_index":null,"parent_total_steps":null,"status":"completed","step_index":0,"step_name":"main","step_source":null,"step_type":"agent","total_steps":1,"workflow_name":"tmp_260824_135856"}}]
