#gh:gh_sase-org__sase Can you help me give service proc nodes on the "Services" tab a sticky description
header which is shown when that service proc node is selected and can be
expanded/collapsed using the `d` keymap? See how we do this for routine and job nodes
for context and inspiration. #beau

#plan %m:@xlarge