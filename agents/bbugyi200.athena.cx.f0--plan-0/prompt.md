#gh:gh_sase-org__sase #fork:cx This mostly worked, but when I press `l` twice to show hidden steps, it looks like those steps get grouped separately from the clan and agent/family they are associated with, which should never happen since clan and agent/agent family membership should take precedence over this grouping strategy (see #sshot for context). Can you help me fix this? #plan