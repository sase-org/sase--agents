#gh:gh_sase-org__sase #fork:mr One thing we should start folding (and only showing on level 2) for both agents and agent families is the `SLOW TOOL CALLS` section entry full command (see #sshot for what level 1 folding of this section should look like). Can you help me implement this?

- Make sure that we properly indent and line wrap long commands.
- Add some more useful information about the slow tool calls to the level 2 fold view as well. Use your best judgment here.
- #beau 

#plan #m_opus