#gh:gh_sase-org__sase %w:sase-yf.land Can you help me add support to external editors (via LSP
support) for the new `*` functionality that we just added to the prompt input widget
(see the sase-yf epic bead for context)? Make sure the LSP support we add shares as much
code with the corresponding functionality for the prompt input widget as possible.

#plan