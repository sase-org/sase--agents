#gh:gh_sase-org__sase The `gy.f1.f6.f0.w0.f2` sase agent (shown in #sshot) is only showing `gh` and `fork` in the agent metadata panel's `Xprompts:` field, but it also invoked the `beau` and `plan` xprompts in its prompt. Can you help me diagnose the root cause of this issue and fix it? #plan