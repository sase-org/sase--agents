#gh:gh_sase-org__sase %w:sase-yf.land.w0 Can you help me extend the new `*` functionality that we
added to the prompt input widget and external editors (via LSP support) by adding a new
`**` functionality that acts in a similar way, but is triggered when the user types `**`
after a space or at the start of a line?

- See the sase-yf epic bead for context on the `*` functionality (we added LSP support
  as a follow up after this epic was completed).
- Instead of showing a completion menu for model aliases, we should show a completion
  menu for explicit models when this functionality is triggered.
- #beau 

#plan %m:@xlarge