#gh:gh_sase-org__sase The "sase" sase project keeps getting converted to a bare-git project (i.e. the `WORKSPACE_DIR` key in the ~/.sase/projects/gh_sase-org__sase/gh_sase-org__sase.sase file is changed to point to a ~/projects/git/ directory and the `BARE_REPO_DIR` key is added). I'm not sure what's causing this. Can you help me diagnose the root cause of this issue and fix it so this doesn't happen anymore? #plan #m_opus