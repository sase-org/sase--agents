#gh:gh_sase-org__sase Can you help me have the `sase plan validate` command start instructing (in comments or however we provide hints to the agent using this command) sase agents to make sure that each `phase` property entry briefly references the phase section in the epic plan file (not the epic plan file itself, since that will be shown by the `sase bead show` command) and provides a good summary of that phase section's contents? Keep these instructions concise and useful. Remember that every token in context either helps or hurts us. #plan #m_fable