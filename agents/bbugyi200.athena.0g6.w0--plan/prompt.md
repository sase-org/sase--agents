#gh:gh_sase-org__sase %w:0g6 Can you help me add a third section to the agent instruction files that
get generated by the `sase init` command that contains all of the generated memory web
sub-sections?

- Let's stop using the "Tier 1" and "Tier 2" conventions for core and reference
  memories, respectively. The memory webs section will not necessarily be rendered in
  every agent instruction file in every project and it should be rendered as the second
  section. So I don't want some agent instruction files containing a tier 3 section but
  no tier 2 section or some agent instruction files using different tier numbers to
  refer to different types of sase memory.
- Make sure that you remove all related "tier 1" / "tier 2" references found in this
  codebase (and linked repos, if any of those contain references).
- This means for the "sase" sase project, for example, the glossary, decisions, and
  task_types memory webs should each have a dedicated (numbered) sub-section under this
  new memory webs section.
- Make sure to update the "SASE Memory" section at the top of the sase/memory/sase.md
  file as well as any other related instructions that need updating to support this
  change.
- I'm pretty sure memory webs currently need to be configured as either core memory or
  reference memory. We will need to drop this configuration in order to support this
  change. Memory webs should always be rendered in this new memory web section.

#plan #m_opus