#gh:gh_sase-org__sase #fork:0s Can we actually omit the provider part of this text? Instead, we
should use the smallest string that the `%model` directive would accept. For example,
let's start showing `grok-4.6@high` instead of `GROK(grok-4.6)@high`. #plan