#gh:gh_sase-org__sase #fork:2t The output of the `sase vcs log` command is showing invalid project names (see #sshot). For example, as we have gone over MANY times, the user should NEVER see a project spec file name like "gh_sase-org__sase". Instead, they should see the project name (configured with the PROJECT_NAME field in the project spec file), which is "sase" in this example. Can you help me diagnose the root cause of this issue and fix it? #tale #m_opus