#gh:gh_sase-org__sase We recently improved the way custom user-specified sase gate command inputs/arguments are handled/supported (see the sase-h7 epic bead for context). Can you now help me make it so that, if the user selects an option that has required inputs (like shown in #sshot, when the user pressed `3`), that the first required input is focused instead of displaying an error toast? #plan