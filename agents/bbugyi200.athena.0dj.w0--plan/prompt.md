#gh:gh_sase-org__sase %w:0dj The `task_types` memory web (see the sase-sq epic bead for context) is the only web that currently recommends agents to run a command other than the `sase memory read` command to read its strands. Can you help me fix this? Make sure that the strand files (which should be auto-generated by the `sase init` command based on which task bead types are enabled for this project / what plugins are installed) contain the same information that is currently provided by the `sase bead task-type show` command.

#plan