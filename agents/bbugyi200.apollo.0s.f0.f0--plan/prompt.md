#gh:gh_sase-org__sase #fork:0s.f0 Can you now help me start rendering this text using a distinct
color depending on which LLM provider it is associated with (I think we already do this
elesewhere for LLM provider text, so re-use those colors). #beau

#plan