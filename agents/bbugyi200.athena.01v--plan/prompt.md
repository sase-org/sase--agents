#gh:gh_sase-org__sase Can you help me make the `K` keymap in the prompt input widget stop showing the preview for an xprompt when one of the `#foo: Some single-line arg...` or `#foo:: Some multi-line arg...` argument forms are used and part of the single/multi-line argument is selected (we should prefer all other functionalities of the `K` keymap before defaulting to this behavior)? #plan