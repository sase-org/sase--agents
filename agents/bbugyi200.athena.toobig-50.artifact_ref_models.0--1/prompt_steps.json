[{"file_name":"prompt_step_main.json","marker":{"embedded_workflow_name":null,"hidden":false,"is_pre_prompt_step":false,"output":{"_raw":"I’m picking this up from the failed `check-full` and first verifying whether the single failure is related to the refactor or to a pre-existing contract mismatch. I’ll also read the required SASE verification/finalization instructions before closing this out.\n\nThe working tree contains only the expected artifact reference model split so far. I’m pulling the full failure details now, because the tail only names the failing contract test and not the actual diff it saw.\n\nThe full monitor output confirms the `check-full` setup rebuilt `sase_core_rs` from a newer linked core checkout before tests ran. Next I’m reading the repo’s verification rules and rerunning the failing test directly so the assertion details are visible.\n\nThe split itself is within the size target: the facade is 95 lines and the new modules range from 66 to 209 lines. The targeted failing test is still running; once it prints the assertion I can separate contract drift from refactor fallout cleanly.\n\nThe contract failure is a core/runtime vocabulary mismatch: the rebuilt core contract now includes `queue`, while the Python runtime directive table does not. That is separate from the artifact reference split, so I’m doing a scoped diff/read of the refactor rather than touching the xprompt directive code in this task.\n\nThe refactor modules read as a straight split: shared wire helpers, parsed/reference payloads, caller context, entries, resolution, scans, and document target resolution. I’m comparing against the original tracked file now to catch any accidental semantic change beyond moving definitions.\n\nThe original implementation used the same wire constants, dataclasses, validation checks, and lazy imports. The only observed change is module placement plus facade re-exports, so I’m running the artifact reference test slice and an import/compile sanity check.\n\nThe facade import smoke and compile pass. There are still many existing imports through `sase.artifact_ref_models`, which is exactly why the facade is useful here.\n\nThe artifact reference slice passed: `111 passed in 3.37s`. I’m doing final status/diff checks now; unless those surface something unexpected, I’ll finalize with the full-suite failure called out as unrelated xprompt/core drift.\n\nI’ve finished the ordinary verification I can do in scope: lint/check-full got through the refactor-relevant gates, `tests/artifact_refs` passes, and the remaining full-suite failure is `queue` being present in the core xprompt directive contract but absent from the Python runtime table. I’m now doing the required SASE final declaration.\n\nThe finalizer context requires a commit declaration for the main repo’s artifact-ref split files. I’m submitting that now with a scoped refactor message; after a successful submit I won’t run more commands.\n\nImplemented the split and kept the compatibility facade at [artifact_ref_models.py](/home/bryan/.local/state/sase/workspaces/sase-org/sase/sase_17/src/sase/artifact_ref_models.py:1).\n\nNew modules:\n`artifact_ref_wire.py`, `artifact_ref_parsed_models.py`, `artifact_ref_context_models.py`, `artifact_ref_entry_models.py`, `artifact_ref_resolution_models.py`, `artifact_ref_scan_models.py`, `artifact_ref_target_models.py`.\n\nAll split files are under 500 lines; largest is 209 lines.\n\nVerification:\n- `tests/artifact_refs -q`: `111 passed`\n- `compileall` on all split modules: passed\n- facade import smoke: passed\n- prior `just check`: passed\n- monitored `just check-full`: failed on unrelated `tests/test_xprompt_directive_contract.py::test_runtime_directive_vocabulary_matches_core_contract`\n\nThe full-suite failure is not from this split: rebuilt core now exposes directive `queue`, while the Python runtime directive vocabulary does not. I left that xprompt/core contract drift untouched because it is outside the artifact model refactor. The SASE final declaration was accepted for committing the refactor."},"parent_step_index":null,"parent_total_steps":null,"status":"completed","step_index":0,"step_name":"main","step_source":null,"step_type":"agent","total_steps":1,"workflow_name":"tmp_260908_204459"}}]
