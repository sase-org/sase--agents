#gh:gh_sase-org__sase Can you help me give dashes that are used as bullets (i.e. the `-` in `- ` if `- ` is at the start of the line with nothing to the left of it but potentially spaces) a better foreground color in the prompt input widget so it is easy for users to spot them? Make sure this color is highly visible, appropriate, and doesn't clash with existing colors used in the prompt input widget. #bea #m_opus