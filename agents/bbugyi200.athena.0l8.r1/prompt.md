%id:0l8.r1 #gh:gh_sase-org__sase I recently canceled the sase-113 epic bead, which proposed several
renames. I'm in the process of having several agents address this separately instead.
You are one of these agents. Namely, can you help me rename the `sase axe` command to
`sase supervisor`? Make sure to migrate all "AXE" references to "supervisor" or "sase's
supervisor". Update ALL user-facing references (internal references to axe can be left
as is for now).

#plan %m:gpt-6-astra %w(bead=sase-11e)