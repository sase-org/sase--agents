%id:0l8.r0 #gh:gh_sase-org__sase I recently canceled the sase-113 epic bead, which proposed several
renames. I plan on having several agents address this separately instead. I started with
the `0l8` sase agent's rename of `sase ace` to `sase tui`. I want you to handle the next
part. Namely, can you help me rename "lumberjacks" to "routines" and "chops" to "jobs"
(let's leave the "AXE" tab and the `sae axe` command alone for now, but make sure to
rename the `sase axe lumberjack/chop` commands appropriately and update all other
user-facing references)? You should only replace user-facing references; internal
references to ace can stay for now.

#plan %m:gpt-6-astra