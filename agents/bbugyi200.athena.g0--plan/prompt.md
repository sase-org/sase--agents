#gh:gh_sase-org__sase Something keeps causing this machine to take up way too much CPU resources (the `uptime` command was showing values in the thousands). I suspect it has something to do with this project's test suite. Can you dig into this, diagnose the root cause, and fix it? #plan #m_fable