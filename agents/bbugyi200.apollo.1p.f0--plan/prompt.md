#gh:gh_sase-org__sase #fork:1p Can you now help me add support for pressing the corresponding capital
letter on that new panel to open that deck in a new horizontal deck panel (or in the
non-focused deck panel if another panel already exists--if a verttical deck layout is
currently used, don't change it to horizontal; just update the deck shown in the other
deck panel)? #plan %m:opus@xhigh