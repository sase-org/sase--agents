#gh:gh_sase-org__sase Sase's memory webs use markdown comments of the form `<!-- sase:strands -->` to indicate where strand file names, keywords, and descriptions should be rendered. This is fine (and important) when it comes to the memory files themselves, but we should be able to strip these lines (and the blank lines that preceed these lines) from the markdown that gets rendered in agent instruction files, right? #if_so_plan