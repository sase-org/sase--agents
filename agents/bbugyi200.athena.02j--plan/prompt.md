#gh:gh_sase-org__sase The `bob-cli-t.4.5.3` sase agent should have closed the bob-cli-t.4 epic bead, but did not. I think this is probably because the `sase bead close` command requires the `--force` option to close a bead with open children and/or because agents that work epic phases are instructed (in the `#bd/work_phase_bead` xprompt) to only close their own bead and not their epic bead. Can you help me confirm/deny my suspicion, diagnose the true root cause, and fix the issue? #plan