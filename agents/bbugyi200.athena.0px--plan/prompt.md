#gh:gh_sase-org__sase We seem to have replaced the `project:<project>` prompt history filter with
`+<project>` when the user uses the `<ctrl+k>` keymap in the prompt input widget (see
the sase-16n epic bead and the project tags glossary entry for context on the
`+<project>` syntax). This is not correct. Does the sase-16n.11 epic bead address this
issue? #if_not_plan %m:@xlarge