#gh:gh_sase-org__sase Can you help me add support for a new `gf` (normal mode) / `<ctrl+g>f` (insert mode) keymap in the prompt input widget that formats (using `prettier` or however we currently format prompts--use the same process to make sure the way we format prompts is consistent across the codebase) the prompt that is currently contained in the currently selected prompt input widget? #tale