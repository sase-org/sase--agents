#gh:gh_sase-org__sase Can you help me make the `g` (group) keymap on the "Statistics" tab of the "SASE Admin Center" panel only visible on the sub-tabs of that panel that actually support that keymap? Also let's add a new `P` keymap to this tab that allows the user to toggle which project we are filtering for which should work just like the `p` keymap except for it should toggle projects in the opposite direction. #plan