#gh:gh_sase-org__sase #fork:0ar I tried pasting in the JSON output by the `sase machine bootstrap --json` command on that machine, but that resulted in the below error. Do you understand what caused this?
```
failed to enroll https://apollo.tail297af1.ts.net: fleet gateway request failed: URLError
```