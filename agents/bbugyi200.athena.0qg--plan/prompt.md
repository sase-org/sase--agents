#gh:gh_sase-org__sase We should always show all agent jump targets when the jump target panel is
expanded. These should never be folded (see the `… +8 more neighbors (zz to show more)`
text in the ~/tmp/screenshots/20260923_210418.png screenshot for context). Can you help
me fix this? #plan