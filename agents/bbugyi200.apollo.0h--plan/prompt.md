#gh:gh_sase-org__sase I can't really see the recently added `<current_match>/<total_matches>`
indicator shown when using the search functionality (triggered via the `/` keymap) in
the prompt input widget (see the ~/tmp/screenshots/20260918_063722.png screenshot for
context). Can you help me fix this by using better colors and/or better syntax
highlighting? #bea

#plan %m:opus