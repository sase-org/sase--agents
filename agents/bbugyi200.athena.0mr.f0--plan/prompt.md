#gh:gh_sase-org__sase #fork:0mr I've run into a different issue (see the command output below for
context--running the `uv pip install -e '.[dev,visual]'` command did not fix the issue).
Make sure this works by running the `sase screenshot` command when you are done. #plan
```
❯ sase screenshot
sase screenshot: ACE SVG-to-PNG rendering requires the visual extra. Install it with `uv pip install -e '.[dev,visual]'` or an equivalent environment setup.
```