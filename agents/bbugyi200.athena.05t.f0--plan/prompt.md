#gh:gh_sase-org__sase #fork:05t Can you now help me make this command much more robust and reliable (see the command output below for context)? #plan 
```
❯ sase agent restart 061
Forced name reuse did not survive prompt rewrite; the stored prompt has no reusable %id.
Add an explicit %id or use ACE's ,x.
```