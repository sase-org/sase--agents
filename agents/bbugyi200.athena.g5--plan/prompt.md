#gh:gh_sase-org__sase After updating sase from the TUI, when the TUI restarts the expansion state of each agent tribe on the agents tab is forgotten (they all show as expanded). We seem to correctly remember Agent Tribe expansion state between TUI restarts but when we update it seems the expansion state is lost. Can you help me diagnose the root cause of this issue and fix it? #plan