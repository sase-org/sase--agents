#gh:gh_sase-org__sase #fork:0e Can you now help me add a new layout option that uses a 50/50 split
(e.g. equal height) for the metadata panel and the file/LLM Calls panels?

- Let's keep using the `p` key to toggle to the next layout but let's also add a new `P`
  key to toggle to the previous layout (since there are now 3 layouts instead of 2).
- Also, let's start using `0` as the key associated with hiding the file/LLM Calls
  panels instead of `n`.

#plan