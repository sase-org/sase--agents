#gh:gh_sase-org__sase #fork:0j8 Can you now help me remove the `~` that is currently next to the
`<N>%` in usage windows? Also, let's start rendering the background of `<N>%` red when
`<N>` equals `0` (i.e. when the usage window has hit a limit). #plan