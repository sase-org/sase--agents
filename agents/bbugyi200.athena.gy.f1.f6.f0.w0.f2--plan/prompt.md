#gh:gh_sase-org__sase #fork:gy.f1.f6.f0.w0 Can you now help me add support for editing (in the user's chezmoi repo if `use_chezmoi` is set) and/or overriding the default model effort level from this panel using a new keymap (`<ctrl+e>` if it is available), which should trigger a little panel that allows the user to select (with a single keypress) whether they would like to edit or override the current effort level (which should be shown on this little panel)? In both cases, we should show a nice new panel with all available effort levels to the user (allowing them to select a new effort level with a single keypress). If the user selects override then we should present them with the same options/panel that we do when overriding model aliases from the "Models" panel.

#beau #plan