#gh:gh_sase-org__sase #fork:gy.f1 %w:gy.f1.f1 Can you now help me define a new model alias fallback syntax using `||`? For example, a model alias that uses `A || B` should always render to `A` unless the agent CLI provider for `A` is not installed, in which case we should fallback to `B`. As our first use case, let's start defining the builtin `@smartest` model alias with a default value of `claude/claude-fable-5 || codex/gpt-5.6-sol` (then remove the corresponding `smartest` config field in the sase.yml file in my chezmoi repo--this isn't needed anymore since fable 5 will now be used by the `@smartest` alias by default). #plan