%wait(sase-ri.land.w2.f3)
#gh:gh_sase-org__sase Can you help me make it so the filter bar on the "XPrompts" sub-tab of the "Config" tab of the "SASE Admin Center" panel is only shown when a new `/` keymap is used on that tab? This matches how filtering works on other tabs/sub-tabs in sase's TUI better I think.

#beau #plan