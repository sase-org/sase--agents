- **PLAN:** [202605/disable_kitty_cmd_f.md](https://github.com/sase-org/sase--plans/blob/main/202605/disable_kitty_cmd_f.md)

 #resume:anq.code This worked for the `<cmd+b>` keymap but not the `<cmd+f>` keymap since it seems to map to text search in kitty. Can you help me fix this by disabling that kitty keymap? Think this through thoroughly and create a plan using your `/sase_plan` skill before making any file changes.
